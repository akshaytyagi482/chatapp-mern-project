<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="../stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-max min-h-full flex flex-col gap-5 p-10 bg-zinc-900">
        <a class="text-blue-500" href="/"> <- Back to create user</a>
       <div class="text-white text-4xl">
        Database
       </div>
       <div class="w-full h-full flex flex-wrap gap-3 p-5">
        <% if(users.length == 0) {%>
            <div>Add some pics pookie</div>
            <%}%>
        <% users.forEach(function(e){ %>
                <div class="card w-72 rounded-lg gap-2 bg-blue-800 flex flex-col p-4 h-max">
                <img src="<%= e.image %>" class="w-full h-3/4 object-cover border border-black" alt="">
                <div class="text-2xl"><%=e.name %></div>
                <div class="text-xl"><%= e.email %></div>
                <div class="flex gap-5">
                   <a href="./edit/<%= e._id %>" class="text-black rounded-lg decoration-none p-3 bg-yellow-500" >edit user </a>
                   <a class="bg-red-400 text-black p-3 rounded-lg decoration-none" href="./delete/<%= e._id %>">delete user</a>
                </div>
            </div>
       <% }) %>
       </div>

    </div>
</body>
</html>